import 'package:flutter/material.dart';

class RuleManagementScreen extends StatelessWidget {
  const RuleManagementScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Manage Categorization Rules'),
      ),
      body: const Center(
        child: Text('Rule management functionality will be implemented here.'),
      ),
    );
  }
}
